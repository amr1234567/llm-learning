---
import Layout from '../../layouts/Layout.astro';
---

<Layout>
    <div class="container">
        <h1>🧩 Elements of a Prompt</h1>
        <p class="subtitle">
            Every great prompt is built from up to 4 ingredients — learn what
            they are and how to use them
        </p>
        <div class="doc-link">
            <a
                href="https://www.promptingguide.ai/introduction/elements"
                target="_blank">📄 View Original Docs — promptingguide.ai</a
            >
        </div>

        <!-- The 4 Elements -->
        <div class="section-title">🧱 The 4 Building Blocks</div>
        <div class="elements-grid">
            <div class="el-card instruction">
                <div class="el-icon">📋</div>
                <div class="el-name">1. Instruction</div>
                <div class="el-def">
                    The specific task or action you want the AI to perform. This
                    is the "verb" of your prompt — what should the AI <em>do</em
                    >?
                </div>
                <div class="el-hint">
                    🧠 "Classify…", "Summarize…", "Translate…", "Write…"
                </div>
            </div>

            <div class="el-card context">
                <span class="optional-badge">Optional</span>
                <div class="el-icon">🌍</div>
                <div class="el-name">2. Context</div>
                <div class="el-def">
                    Background info or extra details that help the AI understand
                    the situation better and give a more accurate answer.
                </div>
                <div class="el-hint">
                    🧠 Like giving a friend background before asking for advice
                </div>
            </div>

            <div class="el-card input">
                <div class="el-icon">📥</div>
                <div class="el-name">3. Input Data</div>
                <div class="el-def">
                    The actual content the AI should work with — the text,
                    question, or data you want it to process or respond to.
                </div>
                <div class="el-hint">
                    🧠 "Text: I think the food was okay." — this is the input
                </div>
            </div>

            <div class="el-card output">
                <span class="optional-badge">Optional</span>
                <div class="el-icon">📤</div>
                <div class="el-name">4. Output Indicator</div>
                <div class="el-def">
                    Tells the AI what format or type of answer you expect — a
                    label, a list, a JSON object, a single word, etc.
                </div>
                <div class="el-hint">
                    🧠 "Sentiment:", "Answer:", "JSON:", "In one sentence:"
                </div>
            </div>
        </div>

        <!-- Anatomy of a real prompt -->
        <div class="section-title">🔍 Real Prompt — Colour-Coded Anatomy</div>
        <div class="anatomy-box">
            <span class="part instruction"
                >Classify the text into neutral, negative, or positive</span
            ><br /><br />
            <span class="part context"
                >The reviewer is a food critic who visited the restaurant last
                week.</span
            ><br /><br />
            <span class="part input">Text: I think the food was okay.</span><br
            /><br />
            <span class="part output">Sentiment:</span>
        </div>
        <div class="legend">
            <div class="legend-item">
                <div class="legend-dot dot-instruction"></div> Instruction
            </div>
            <div class="legend-item">
                <div class="legend-dot dot-context"></div> Context
            </div>
            <div class="legend-item">
                <div class="legend-dot dot-input"></div> Input Data
            </div>
            <div class="legend-item">
                <div class="legend-dot dot-output"></div> Output Indicator
            </div>
        </div>

        <!-- Interactive Builder -->
        <div class="section-title">🛠️ Build Your Own Prompt</div>
        <div class="builder">
            <p class="builder-intro">
                Fill in the boxes below and click <strong style="color:#fff;"
                    >Build Prompt</strong
                > to assemble a well-structured prompt. You don't need all 4 — only
                Instruction and Input are required.
            </p>

            <div class="field-group">
                <div class="field-label">
                    <span class="badge badge-instruction">Instruction</span>
                    What should the AI do?
                </div>
                <input
                    type="text"
                    id="f-instruction"
                    placeholder='e.g. "Summarize the following text in 3 bullet points"'
                />
            </div>

            <div class="field-group">
                <div class="field-label">
                    <span class="badge badge-context">Context</span>
                    <span class="badge badge-opt">Optional</span>
                    Any background info?
                </div>
                <input
                    type="text"
                    id="f-context"
                    placeholder='e.g. "This is a customer complaint email from a frustrated user"'
                />
            </div>

            <div class="field-group">
                <div class="field-label">
                    <span class="badge badge-input">Input Data</span>
                    What content should the AI work on?
                </div>
                <textarea
                    id="f-input"
                    rows="3"
                    placeholder="e.g. &quot;I ordered a laptop 2 weeks ago and it still hasn't arrived. The support team is not responding.&quot;"></textarea>
            </div>

            <div class="field-group">
                <div class="field-label">
                    <span class="badge badge-output">Output Indicator</span>
                    <span class="badge badge-opt">Optional</span>
                    What format should the answer be in?
                </div>
                <input
                    type="text"
                    id="f-output"
                    placeholder='e.g. "Summary:", "Tone:", "Answer in JSON:", "In one sentence:"'
                />
            </div>

            <button class="build-btn" onclick="buildPrompt()"
                >⚡ Build Prompt</button
            >

            <div class="result-box" id="result-box">
                <div class="result-label">YOUR ASSEMBLED PROMPT</div>
                <pre id="result-text"></pre>
                <button class="copy-btn" onclick="copyPrompt()"
                    >📋 Copy to clipboard</button
                >
            </div>
        </div>

        <div class="tip-banner">
            <span style="font-size:1.4rem; flex-shrink:0;">💡</span>
            <p>
                <strong>Key Takeaway:</strong> You don't need all 4 elements every
                time. Start with just <strong>Instruction + Input</strong>. Add <strong
                    >Context</strong
                > when the AI seems confused, and an <strong
                    >Output Indicator</strong
                > when you need a specific format like a list, JSON, or a single label.
            </p>
        </div>
    </div>
</Layout>

<style>
    .container {
        padding: 2rem;
        max-width: 860px;
        margin: 0 auto;
    }
    h1 {
        text-align: center;
        font-size: 1.8rem;
        margin-bottom: 0.3rem;
        color: #fff;
    }
    .subtitle {
        text-align: center;
        color: #94a3b8;
        margin-bottom: 0.8rem;
        font-size: 0.95rem;
    }
    .doc-link {
        text-align: center;
        margin-bottom: 2rem;
    }
    .doc-link a {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: #1e2130;
        border: 1px solid #3b4268;
        color: #818cf8;
        text-decoration: none;
        padding: 0.45rem 1.1rem;
        border-radius: 20px;
        font-size: 0.85rem;
        transition: background 0.2s;
    }
    .doc-link a:hover {
        background: #2d3348;
        border-color: #6366f1;
    }

    .section-title {
        font-size: 1.05rem;
        font-weight: 700;
        color: #a78bfa;
        margin: 2rem 0 1rem;
        display: flex;
        align-items: center;
        gap: 0.6rem;
    }
    .section-title::after {
        content: "";
        flex: 1;
        height: 1px;
        background: #2d3348;
    }

    /* Element cards */
    .elements-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        margin-bottom: 2rem;
    }
    @media (max-width: 580px) {
        .elements-grid {
            grid-template-columns: 1fr;
        }
    }

    .el-card {
        background: #1e2130;
        border-radius: 14px;
        padding: 1.2rem 1.4rem;
        border-top: 4px solid;
        position: relative;
        transition: transform 0.2s;
    }
    .el-card:hover {
        transform: translateY(-2px);
    }
    .el-card.instruction {
        border-color: #6366f1;
    }
    .el-card.context {
        border-color: #059669;
    }
    .el-card.input {
        border-color: #d97706;
    }
    .el-card.output {
        border-color: #e11d48;
    }

    .el-icon {
        font-size: 1.8rem;
        margin-bottom: 0.5rem;
    }
    .el-name {
        font-size: 1rem;
        font-weight: 700;
        color: #fff;
        margin-bottom: 0.3rem;
    }
    .el-def {
        font-size: 0.83rem;
        color: #94a3b8;
        line-height: 1.55;
        margin-bottom: 0.5rem;
    }
    .el-hint {
        font-size: 0.78rem;
        font-style: italic;
        padding: 5px 10px;
        border-radius: 8px;
    }
    .instruction .el-hint {
        background: #1e1f3a;
        color: #a78bfa;
    }
    .context .el-hint {
        background: #1a3a2a;
        color: #4ade80;
    }
    .input .el-hint {
        background: #2a2a1a;
        color: #fbbf24;
    }
    .output .el-hint {
        background: #2a1a1f;
        color: #fb7185;
    }

    .optional-badge {
        position: absolute;
        top: 10px;
        right: 12px;
        font-size: 0.65rem;
        padding: 2px 8px;
        border-radius: 10px;
        background: #2d3348;
        color: #64748b;
        font-weight: 600;
    }

    /* Prompt anatomy */
    .anatomy-box {
        background: #12151f;
        border-radius: 14px;
        padding: 1.4rem;
        margin-bottom: 1.5rem;
        font-family: monospace;
        font-size: 0.88rem;
        line-height: 2.2;
    }
    .part {
        display: inline;
        padding: 2px 6px;
        border-radius: 5px;
        cursor: default;
        transition: opacity 0.2s;
    }
    .part.instruction {
        background: #1e1f3a;
        color: #a78bfa;
        border: 1px solid #6366f133;
    }
    .part.context {
        background: #1a3a2a;
        color: #4ade80;
        border: 1px solid #05966933;
    }
    .part.input {
        background: #2a2a1a;
        color: #fbbf24;
        border: 1px solid #d9780633;
    }
    .part.output {
        background: #2a1a1f;
        color: #fb7185;
        border: 1px solid #e11d4833;
    }

    .legend {
        display: flex;
        flex-wrap: wrap;
        gap: 0.6rem;
        margin-top: 1rem;
    }
    .legend-item {
        display: flex;
        align-items: center;
        gap: 0.4rem;
        font-size: 0.78rem;
    }
    .legend-dot {
        width: 10px;
        height: 10px;
        border-radius: 3px;
    }
    .dot-instruction {
        background: #6366f1;
    }
    .dot-context {
        background: #059669;
    }
    .dot-input {
        background: #d97706;
    }
    .dot-output {
        background: #e11d48;
    }

    /* Builder */
    .builder {
        background: #1e2130;
        border: 1px solid #2d3348;
        border-radius: 14px;
        padding: 1.4rem;
    }
    .builder-intro {
        font-size: 0.85rem;
        color: #94a3b8;
        margin-bottom: 1.2rem;
        line-height: 1.6;
    }

    .field-group {
        margin-bottom: 1rem;
    }
    .field-label {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.82rem;
        font-weight: 600;
        margin-bottom: 0.4rem;
    }
    .field-label .badge {
        font-size: 0.68rem;
        padding: 1px 7px;
        border-radius: 10px;
    }
    .badge-instruction {
        background: #1e1f3a;
        color: #a78bfa;
    }
    .badge-context {
        background: #1a3a2a;
        color: #4ade80;
    }
    .badge-input {
        background: #2a2a1a;
        color: #fbbf24;
    }
    .badge-output {
        background: #2a1a1f;
        color: #fb7185;
    }
    .badge-opt {
        background: #1e2130;
        color: #64748b;
        border: 1px solid #2d3348;
    }

    .field-group input,
    .field-group textarea {
        width: 100%;
        background: #12151f;
        border: 1px solid #2d3348;
        color: #e2e8f0;
        border-radius: 8px;
        padding: 0.65rem 0.9rem;
        font-size: 0.84rem;
        font-family: inherit;
        outline: none;
        resize: vertical;
        transition: border-color 0.2s;
    }
    .field-group input:focus,
    .field-group textarea:focus {
        border-color: #6366f1;
    }

    .build-btn {
        width: 100%;
        padding: 0.75rem;
        border: none;
        border-radius: 10px;
        background: #6366f1;
        color: #fff;
        font-size: 0.9rem;
        font-weight: 700;
        cursor: pointer;
        margin-top: 0.5rem;
        transition: background 0.2s;
    }
    .build-btn:hover {
        background: #4f46e5;
    }

    .result-box {
        margin-top: 1.2rem;
        background: #12151f;
        border-radius: 10px;
        padding: 1.1rem 1.3rem;
        display: none;
    }
    .result-box .result-label {
        font-size: 0.72rem;
        color: #64748b;
        font-weight: 700;
        letter-spacing: 0.08em;
        margin-bottom: 0.6rem;
    }
    .result-box pre {
        font-family: monospace;
        font-size: 0.84rem;
        white-space: pre-wrap;
        line-height: 1.9;
        color: #e2e8f0;
    }
    .result-box .copy-btn {
        margin-top: 0.8rem;
        padding: 0.4rem 1rem;
        border-radius: 8px;
        background: #2d3348;
        color: #94a3b8;
        border: none;
        font-size: 0.78rem;
        cursor: pointer;
        transition: background 0.2s;
    }
    .result-box .copy-btn:hover {
        background: #3b4268;
        color: #e2e8f0;
    }

    .tip-banner {
        background: #1e2130;
        border: 1px solid #3b4268;
        border-radius: 12px;
        padding: 1rem 1.4rem;
        margin-top: 1.5rem;
        display: flex;
        align-items: flex-start;
        gap: 0.8rem;
    }
    .tip-banner p {
        font-size: 0.88rem;
        color: #94a3b8;
        line-height: 1.6;
    }
    .tip-banner strong {
        color: #e2e8f0;
    }
</style>
<script is:inline>
    function buildPrompt() {
        const instruction = document.getElementById("f-instruction");
        const context = document.getElementById("f-context");
        const input = document.getElementById("f-input");
        const output = document.getElementById("f-output");

        if (
            !(instruction instanceof HTMLInputElement) ||
            !(context instanceof HTMLInputElement) ||
            !(input instanceof HTMLTextAreaElement) ||
            !(output instanceof HTMLInputElement)
        ) {
            return;
        }

        const instructionValue = instruction.value.trim();
        const contextValue = context.value.trim();
        const inputValue = input.value.trim();
        const outputValue = output.value.trim();

        if (!instructionValue && !inputValue) {
            alert(
                "Please fill in at least the Instruction or Input Data field.",
            );
            return;
        }

        let parts = [];
        if (instructionValue) parts.push(instructionValue);
        if (contextValue) parts.push("\n" + contextValue);
        if (inputValue)
            parts.push(
                "\n" +
                    (inputValue.includes("\n")
                        ? "Text:\n" + inputValue
                        : "Text: " + inputValue),
            );
        if (outputValue) parts.push("\n" + outputValue);

        const result = parts.join("\n");
        const resultText = document.getElementById("result-text");
        if (resultText) {
            resultText.textContent = result;
        }

        const box = document.getElementById("result-box");
        if (box) {
            box.style.display = "block";
            box.scrollIntoView({ behavior: "smooth", block: "nearest" });
        }
    }

    function copyPrompt() {
        const resultText = document.getElementById("result-text");
        if (!resultText) return;

        const text = resultText.textContent;
        if (!text) return;

        navigator.clipboard.writeText(text).then(() => {
            const btn = document.querySelector(".copy-btn");
            if (!btn) return;
            btn.textContent = "✅ Copied!";
            setTimeout(() => {
                if (btn) {
                    btn.textContent = "📋 Copy to clipboard";
                }
            }, 2000);
        });
    }
</script>
